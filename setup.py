from setuptools import setup, find_packages


setup(name='onlinecp',
      version='0.0a',
      description='A collection of algorithms to perform online change point detection',
      packages=['onlinecp', 'onlinecp/utils'],
      zip_safe=False)
